C# 클래스의 데이터 전달

private, this 키워드 

- 정보은닉
- 게터(Getter)와 세터(Setter)라는 접근 방식을 사용하여 멤버 변수에 접근한다. 
EX) Name 이라는 멤버 변수에 접근하기 위해 SetName과 GetName 메소드를 생성하고, 
호출하는 클래스에서는 멤버변수가 아닌 두 메소드만으로 멤버 변수의 값을 변경해야 한다.

- this 키워드는 자기 자신을 의미한다. 
- setName에서 this.name = name 문장은 매개변수 name의 값을 멤버변수 name에 담으라는 의미이다.